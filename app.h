bool addValue(tree*, unsigned int); // insert value into tree
void showValues(tree*);             // show all values in tree
bool delValue(tree*);               // delete value from tree
void searchValue(tree*);            // search for value
void avg(tree*, int);               // calculate the average of all value in tree
void stdDev(tree*, int);            // calculate the standard deviation of values
void sumVals(node*, double*);       // helper function, sums all values in tree
void sumSqrd(node*, double*, double); // helper function
